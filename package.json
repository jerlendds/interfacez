{
  "name": "interfacez",
  "version": "1.0.0",
  "description": "Built for everybody, because nobody should be lost in the code.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace packages/desktop",
    "package": "npm run package --workspace packages/desktop",
    "make": "npm run make --workspace packages/desktop",
    "build": "npm run make --workspace packages/desktop",
    "publish": "npm run publish --workspace packages/desktop"
  },
  "author": {
    "name": "jerlendds",
    "url": "https://github.com/jerlendds/interfacez"
  },
  "keywords": [
    "interfacez"
  ],
  "license": "UNLICENSED"
}
